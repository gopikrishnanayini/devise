<%= form_for ([@event, @location]) do |f| %>
  <% if @location.errors.any? %>
	<div id='error explanation' >
		<h2><%= pluralize(@location.errors.count,'error')%> Prohibited location from being saved:</h2>
		<ul>
			<% @location.errors.full_messages.each do |message| %>
			<li><%= message %></li>
			<% end %>
		</ul>
	</div>
  <% end %>
	<p>
		<%= f.label :address %>
		<%= f.text_field :address %>
	</p>
	<p>
		<%= f.submit :submit %>
	</p>
<% end %>
